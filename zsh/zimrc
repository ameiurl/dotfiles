# Modules
zmodule environment
zmodule git
zmodule input
zmodule termtitle
zmodule utility

# Prompt related
zmodule duration-info
zmodule git-info
zmodule asciiship
zmodule prompt-pwd
zmodule magicmace

# ✅ 所有只添加 fpath、不调用 compinit 的补全模块放 completion 之前
zmodule zsh-users/zsh-completions

# ✅ completion 在这里，负责执行 compinit
zmodule completion

# ⚠️ 下面是会触发 compinit 的插件，必须放在 completion 之后
zmodule Aloxaf/fzf-tab

# ⚠️ powerlevel10k 会调用 compinit，放 completion 后面
zmodule romkatv/powerlevel10k

# utility plugins
zmodule supercrabtree/k
zmodule hlissner/zsh-autopair
zmodule pabloariasal/zfm
zmodule rupa/z

# 语法高亮系列：必须放在最后！顺序严格遵守官方说明
zmodule zsh-users/zsh-syntax-highlighting
zmodule zsh-users/zsh-history-substring-search
zmodule zsh-users/zsh-autosuggestions
